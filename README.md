The eventual home for a CLI tool to convert Instrument APIs into typed Teal files for validation and use in the writing of measurement scripts.

The generate_tl.lua is a first attempt at writing sucha a tool, but is currently not fully tested, and a output is the Acme file as an example.
